apiVersion: v1
kind: Service
metadata:
  name: {{ include "zarf-api.fullname" . }}
  labels:
    {{- include "zarf-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "zarf-api.selectorLabels" . | nindent 4 }}
