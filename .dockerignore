.git
data/
dist/
charts/
*.md
*.exe
# Local end-to-end test artifacts
e2e-data/
e2e-out/
e2e-src/
# Trim the submodule down to what the build needs is not possible with a
# plain COPY (Go needs the full module), but skip its VCS metadata.
zarf/.git
